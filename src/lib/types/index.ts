import { IconType } from "react-icons";
export type Skill = {
  title: string;
  data: {
    name: string;
    description: string;
    Icon: IconType;
    url: string;
  }[];
};

export type Stack = {
  title: string;
  data: {
    name: string;
    description: string;
    Icon: IconType;
    url: string;
  }[];
};
